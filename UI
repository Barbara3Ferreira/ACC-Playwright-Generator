<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>PO Platform - ACC & Playwright Generator</title>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Roboto', sans-serif;
            display: flex;
            height: 100vh;
            margin: 0;
            background-color: #f4f4f9;
        }

        /* Sidebar */
        .sidebar {
            width: 250px;
            background-color: #2c3e50;
            color: white;
            padding: 20px;
            display: flex;
            flex-direction: column;
            justify-content: space-between;
        }

        .sidebar h2 {
            color: #ecf0f1;
            margin-bottom: 20px;
        }

        /* Main content area */
        .main-content {
            flex-grow: 1;
            padding: 40px;
            overflow-y: auto;
        }

        .container {
            max-width: 800px;
            margin: 0 auto;
        }

        h1, h2 {
            font-weight: 500;
        }

        textarea {
            width: 100%;
            height: 150px;
            margin-bottom: 20px;
            padding: 10px;
            font-size: 16px;
            border: 1px solid #ddd;
            border-radius: 5px;
            background-color: #fff;
        }

        pre {
            background-color: #2d3436;
            color: white;
            padding: 20px;
            border-radius: 5px;
            font-family: monospace;
            white-space: pre-wrap;
            word-wrap: break-word;
            margin-bottom: 30px;
        }

        button {
            background-color: #16a085;
            color: white;
            padding: 10px 20px;
            border: none;
            cursor: pointer;
            border-radius: 5px;
        }

        button:hover {
            background-color: #1abc9c;
        }
    </style>
</head>
<body>

<!-- Sidebar (PO Platform Name Only) -->
<div class="sidebar">
    <h2>PO Platform</h2>
</div>

<!-- Main Content -->
<div class="main-content">
    <div class="container">
        <h1>Acceptance Criteria & Playwright Test Generator</h1>

        <!-- User Story Input -->
        <h2>Step 1: Enter User Story</h2>
        <textarea id="userStory" placeholder="Enter your user story here..."></textarea>

        <button onclick="generateACC()">Generate ACC</button>

        <h2>Step 2: Generated Acceptance Criteria</h2>
        <textarea id="accOutput" placeholder="Your generated ACC will appear here..." disabled></textarea>

        <button onclick="generatePlaywright()">Generate Playwright Test</button>

        <h2>Step 3: Generated Playwright Test</h2>
        <pre id="playwrightTest"></pre>
    </div>
</div>

<script>
// Sample ACC Generation Logic
function generateACC() {
    const userStory = document.getElementById('userStory').value;
    const acc = `
Scenario: User story is implemented
Given the user wants to ${userStory}
When the user performs the relevant actions
Then the system should execute the expected behavior
    `;

    document.getElementById('accOutput').value = acc;
}

// Sample Playwright Test Generation Logic
function generatePlaywright() {
    const acc = document.getElementById('accOutput').value;

    const playwrightTest = `
test.describe('Generated Test', () => {
    test('Test the user story behavior', async ({ page }) => {
        // Parse ACC for exact steps, here’s a basic template:
        // Given the user wants to...
        // When the user performs the relevant actions...
        // Then the system should execute the expected behavior
    });
});
    `;

    document.getElementById('playwrightTest').textContent = playwrightTest;
}
</script>

</body>
</html>
